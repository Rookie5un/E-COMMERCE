# 《基于NLP技术的电商评论多维可视化分析平台》毕业论文重写方案

## 摘要
- 论文按“从零重写、系统实现与算法实验均衡”的路线推进，主标题不变，核心叙事固定为“中文电商评论三分类情感分析 + 多维分析平台落地”，不写成普通电商商城论文。
- 论文证据以仓库真实内容为准：`Flask + Vue 3 + MySQL/JWT + ECharts + reportlab` 的平台闭环，`TextProcessor / SentimentAnalyzer / AspectExtractor / IssueExtractor` 的分析链路，`train_sentiment.py` 的训练脚本，现有模型目录、训练日志、图表源文件和两版初稿仅作为素材参考。
- 当前可直接落地的核心实验事实是：已有训练日志显示验证集 `Accuracy=0.9295`、`Macro-F1=0.8996`；当前论文中未被日志或结果文件证明的数字一律不用，尤其不直接沿用初稿中的 `93.70% / 93.52%` 表述。
- 成稿目标按“答辩级高质量版本”设定：正文约 `1.8 万 - 2.5 万` 字，参考文献 `30-40` 篇，图 `12-18` 幅，表 `8-12` 个，做到结构完整、证据闭环、语言稳健、指标可追溯。

## 论文结构与写法
1. 第一章“绪论”：写研究背景、研究意义、国内外研究现状、现有工作的不足、本文切入点与主要工作，明确本文解决的是“评论洞察效率低、分析结果难结构化和难可视化”的问题。
2. 第二章“相关技术基础”：只写与项目强相关的技术，包括文本清洗、中文 RoBERTa、微调机制、FGM、Focal Loss/Label Smoothing、词典匹配、TextRank、Flask、Vue 3、ECharts、reportlab，不堆砌无关概念。
3. 第三章“系统需求分析”：按当前真实用户定位“商品运营分析员”展开，写业务流程、用例、功能需求、非功能需求，范围限定在商品管理、评论导入、评论管理、分析任务、结果看板、报告导出。
4. 第四章“系统设计”：固定采用“双链路”写法，一条是离线训练链路，一条是在线分析链路；重点写总体架构、模块划分、数据库设计、任务状态流转、模型接入与回退机制、接口分层设计。
5. 第五章“关键算法与模型训练”：单独成章，重点写训练数据构建、标签设计、训练脚本结构、优化策略、实验指标定义和核心对比实验，避免把算法内容塞进系统实现章导致失衡。
6. 第六章“系统实现”：按真实代码写后端 API、服务层、前端页面、图表联动、PDF 报告生成与任务进度展示，突出平台闭环与工程可落地性。
7. 第七章“系统测试与实验分析”：前半写平台功能测试、接口测试、报告生成验证，后半写模型实验与结果分析，形成“工程验证 + 算法验证”双证据结构。
8. 第八章“总结与展望”：总结已完成工作，展望多模型对比、方面级情感、异步任务队列、更多数据源、在线标注迭代等。

## 论文中必须固定描述的接口、对象与素材
- 后端接口固定按五组写：`/api/auth`、`/api/products`、`/api/reviews`、`/api/analysis`、`/api/reports`。
- 核心数据对象固定写这几类：`User`、`Product`、`ReviewBatch`、`Review`、`AnalysisRun`、`ReviewSentiment`、`AspectMention`、`IssueTopic`、`IssueTopicReview`、`Report`。
- NLP 组件固定写这四个：`TextProcessor` 负责清洗与分词，`SentimentAnalyzer` 负责三分类推理与规则兜底，`AspectExtractor` 负责功能点归一化提取，`IssueExtractor` 负责负面问题关键词挖掘。
- 前端页面固定覆盖六个主页面：分析总览、商品管理、数据导入、评论管理、任务管理、分析报告，商品详情页作为补充页面写联动。
- 图表素材优先复用仓库现有源文件：业务流程图、用例图、数据流图、总体架构图、ER 图、活动图、任务时序图、NLP 组件图、训练流程图、部署图。
- 界面截图至少准备：登录页、总览页、导入页、评论管理页、任务详情页、报告详情页、PDF 报告页各 1 张。

## 实验与证据方案
- 主数据集优先使用 `backend/data/train_balanced_full.csv`，其现有摘要为 `77486` 条样本，标签分布为 `negative 30198 / neutral 16641 / positive 30647`；若补实验时间不足，可保留 `train_multiclass_plus_public.csv` 作为对照说明。
- 核心实验固定做三组，保证论文“均衡型”成立：`E0=CE baseline`，`E1=CE + FGM + Early Stopping`，`E2=Focal Loss + FGM + Early Stopping + Class Weight`；如时间允许再补 `E3=Label Smoothing + FGM + Early Stopping`。
- 评价指标固定写 `Accuracy`、`Macro-F1`、各类别 `Precision/Recall/F1`，并单独解释为什么必须强调 `Macro-F1` 和中性类别识别。
- 现有日志中的 `Accuracy=92.95% / Macro-F1=89.96%` 作为当前基准结果写入；新实验只有在生成日志、汇总表或 `training_summary.json` 后才替换正文数字。
- 本地环境当前缺少 `flask`、`flask_jwt_extended`、`pandas`、`torch`，因此实验执行默认走 GPU/远程服务器，直接复用 `backend/scripts/run_ablation_experiments.sh`、`run_ablation_remote.sh`、`collect_ablation_results.py`。
- 第七章结果展示固定包含四类表：数据集统计表、实验配置表、核心对比结果表、类别级分类结果表；固定包含两类图：训练流程图/模型链路图与实验结果柱状图或折线图。

## 执行顺序
1. 先清空旧初稿的行文框架，只保留题目、学校模板和可复用素材，不沿用旧正文叙事。
2. 先整理“证据包”：代码模块说明、接口清单、数据库实体、训练日志、模型目录、图表源文件、页面截图位点。
3. 然后补做核心对比实验，优先拿到一组可复现主结果和两组对比结果，再写第五章和第七章。
4. 实验结果确定后再写第一至第四章，确保摘要、绪论、贡献点、系统设计与实验结论完全一致。
5. 第六章系统实现最后写，用真实页面、真实接口、真实类名和真实字段完成代码级描述。
6. 全文完稿后统一做格式、图表编号、参考文献编号、术语统一和数据一致性校对。

## 验收标准
- 任何性能结论、模块能力和系统流程都能在仓库代码、日志、模型目录或图表文件中找到对应证据。
- 摘要、绪论“主要工作”、结论三处表述一致，不出现“摘要写了、正文没证明”的情况。
- 第五章算法部分和第六章系统部分篇幅接近均衡，不出现“算法一页、系统十页”或反过来的失衡。
- 第七章同时覆盖功能验证和实验分析，不能只有界面截图，也不能只有模型指标。
- 论文中的数据库表、API、页面名称、模型名称、数据规模、指标数字与项目真实实现保持一致。
- 最终检查命令默认写入附录或测试章节：后端单测使用 `python -m unittest discover -s tests -v`，若环境未配齐，则在论文中明确说明验证环境与限制。

## 假设与默认项
- 默认沿用你当前学校模板和题目，不改题。
- 默认按“从零重写”执行，现有两版初稿只作为素材库，不作为最终正文骨架。
- 默认论文风格采取“保守写实”，先用已验证事实，再用补实验结果增强，不写超出证据范围的结论。
- 默认实验优先级高于润色优先级，先把关键对比结果补齐，再打磨语言。
- 默认如果完整消融来不及，不扩大战线，只保留 `1 组主实验 + 2-3 组关键对比`，保证论文真实、完整、可答辩。
